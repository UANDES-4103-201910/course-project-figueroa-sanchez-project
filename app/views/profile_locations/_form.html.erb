<%= form_with(model: profile_location, local: true) do |form| %>
  <% if profile_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_location.errors.count, "error") %> prohibited this profile_location from being saved:</h2>

      <ul>
      <% profile_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :profile %>
    <%= form.text_field :profile %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
