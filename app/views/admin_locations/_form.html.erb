<%= form_with(model: admin_location, local: true) do |form| %>
  <% if admin_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_location.errors.count, "error") %> prohibited this admin_location from being saved:</h2>

      <ul>
      <% admin_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
